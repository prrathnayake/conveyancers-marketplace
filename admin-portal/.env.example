# The admin portal now reads configuration from the shared repository .env file.
# Duplicate ../.env.example as ../.env and edit the values there for local development.
# Define only portal-specific overrides in this file if required.
