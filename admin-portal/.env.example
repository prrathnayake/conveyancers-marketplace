# Shared JWT secret for signing session cookies.
JWT_SECRET=replace-with-strong-secret

# Optional: expose the public URL for the customer-facing site when linking back.
NEXT_PUBLIC_MAIN_APP_URL=http://localhost:5173

# Administrator bootstrap credentials.
ADMIN_SEED_EMAIL=admin@example.com
ADMIN_SEED_PASSWORD=change-me-strong
# Alternatively, provide a precomputed bcrypt hash (takes precedence over ADMIN_SEED_PASSWORD).
# ADMIN_SEED_PASSWORD_HASH=$2a$12$examplehash

# When running the portal outside of the frontend directory you can force a shared data path.
# PLATFORM_DATA_DIR=../frontend/data
