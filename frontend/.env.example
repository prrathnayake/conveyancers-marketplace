# The frontend now sources environment variables from the repository root .env file.
# Copy ../.env.example to ../.env and update values there.
# You can still define overrides here if you want app-specific values.
