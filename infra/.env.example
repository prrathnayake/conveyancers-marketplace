# Infrastructure services now look to the repository root .env file for configuration.
# Copy ../.env.example to ../.env and customise those values instead of maintaining this file.
# You may still create an infra/.env file if you need docker-compose specific overrides.
