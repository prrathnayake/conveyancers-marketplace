# === Frontend ===
NEXT_PUBLIC_API_BASE=https://localhost
# === DB/Cache ===
POSTGRES_URL=postgres://app:appsecret@postgres:5432/convey
REDIS_URL=redis://redis:6379
# === Object storage (MinIO/S3) ===
S3_ENDPOINT=http://minio:9000
S3_ACCESS=minio
S3_SECRET=minio12345
S3_BUCKET=convey-docs
# === Auth ===
JWT_SECRET=change-me
JWT_REFRESH_SECRET=change-me-too
# === Integrations (replace in production) ===
PSP_PROVIDER=sandbox
PSP_SECRET=replace-me
KYC_PROVIDER=sandbox
KYC_WEBHOOK_SECRET=replace-me
ESIGN_PROVIDER=sandbox
ESIGN_WEBHOOK_SECRET=replace-me
