# Environment values for docker-compose are now centralised in the repository root .env file.
# Copy ../../.env.example to ../../.env and export from there. This file is retained only for backward compatibility.
